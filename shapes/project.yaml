# A project that groups tasks. Tasks belong to projects.
#
# Sources: Linear, Todoist
project:
  identity: [platform, id]
  subtitle: state
  fields:
    state:             string
    color:             string
