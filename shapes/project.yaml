# A project that groups tasks. Tasks belong to projects.
#
# Sources: Linear, Todoist
project:
  fields:
    state:             string
    color:             string
  display:
    title: name
    subtitle: state
    columns:
      - name:            Project
      - state:           State
