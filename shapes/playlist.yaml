# A video playlist. Playlists are lists that contain videos instead of products.
#
# Sources: YouTube
playlist:
  also: [list]        # a playlist is also a list
  relations:
    contains:          video[]
  display:
    title: name
    subtitle: text
    image: image
    columns:
      - name:            Playlist
