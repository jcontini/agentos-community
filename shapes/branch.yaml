# A git branch.
#
# Sources: git
branch:
  plural: branches
  subtitle: commit
  fields:
    commit:            string    # HEAD commit hash
    upstream:          string    # remote tracking branch
    ahead:             integer
    behind:            integer
    isCurrent:        boolean
    isRemote:         boolean
  relations:
    repository:        repository
