# Base type for anything that can be attributed as "who did this" in the graph.
# Not used directly — person, organization, and agent extend it via `also`.
actor:
  subtitle: actorType
  fields:
    actorType:        string    # person, organization, or agent
