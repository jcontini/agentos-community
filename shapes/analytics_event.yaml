# A product analytics action — a single user interaction tracked by PostHog.
# NOT a life event — see event.yaml for births, marriages, etc.
#
# Sources: PostHog
analytics_event:
  fields:
    distinct_id:       string    # PostHog user identifier
    properties:        json      # arbitrary event properties
    current_url:       url       # page URL where the action fired
  relations:
    person:            person
  display:
    title: name
    date: datePublished
    columns:
      - name:            Event
      - datePublished:   Time
      - distinct_id:     User
