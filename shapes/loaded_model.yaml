# A currently loaded/running AI model instance.
#
# Sources: Ollama
loaded_model:
  subtitle: size
  fields:
    size:              string    # memory footprint
    quantization:      string
    expiresAt:        datetime
    vramUsage:        string
    sizeVram:         integer   # VRAM bytes used by this model
    digest:            string    # content-addressable hash
