# Milestone — A Significant Outcome on a Roadmap
#
# A milestone marks a meaningful point of progress.
# Inherits all properties from outcome.

id: milestone
plural: milestones
name: Milestone
extends: outcome
description: A significant outcome on a roadmap

display:
  primary: name
  icon: flag
  sort:
    - field: data.priority
      order: asc
    - field: name
      order: asc
