# Place — Putting things in containers
#
# The universal containment pattern. Placing content on a platform,
# adding items to collections, filing into categories, joining groups.
# From PROV-O: hadMember. CIDOC-CRM: E9 Move, E10 Transfer.
# Dublin Core: hasPart.
#
# Abstract — never used directly. Children: upload, publish, add_to,
# set, file_in, tag, post, join, includes.

id: place
is_relationship: true
name: Place
description: Putting things in containers — the universal containment pattern

from_type: place

properties:
  position:
    type: integer
    description: Ordering position within the container
