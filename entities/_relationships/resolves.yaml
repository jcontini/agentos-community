# Resolves — A Decision Resolves a Question
#
# The link between a decision (outcome subtype) and the question
# it answers. A question may be resolved by multiple decisions
# over time (superseding each other).

id: resolves
name: Resolves
description: A decision resolves a question

is_relationship: true

from_type: decision
to_type: question

properties:
  rationale:
    type: string
    description: Why this decision resolves the question
