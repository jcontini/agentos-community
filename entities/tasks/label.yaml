# Label entity

id: label
plural: labels
name: Label
description: A tag or category that can be applied to tasks for organization

references:
  schema_org: https://schema.org/DefinedTerm
  wikidata: Q1076869

properties:
  id:
    type: string
    required: true
    description: Unique identifier

  name:
    type: string
    required: true
    description: Label name

  color:
    type: string
    description: Display color (hex or named)

  created_at:
    type: datetime
    description: When label was created

operations: [list, get, create, delete]

display:
  primary: name
  icon: tag
  show_as_app: false
