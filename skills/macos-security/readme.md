---
id: macos-security
name: macOS Security
description: "Audit macOS credentials, Keychain entries, and app tokens. Lists what OAuth tokens, API keys, and session credentials are stored on this machine, extracts specific tokens, identifies which apps have Google OAuth access, and scans macOS Internet Accounts (the OS-level connections that appear as "macOS" in Google's authorized apps list).
"
color: "#6B7280"
website: "https://support.apple.com/guide/keychain-access"
---
